// @flow
export {
  assertDataType,
  assertDataTypeClass,
  castData,
  getConstructor,
  getConstructorName,
  getDataLength,
  assertNotEmptyString,
  assertB64StringWithSize
} from './data-types';

export type { Data } from './data-types';
