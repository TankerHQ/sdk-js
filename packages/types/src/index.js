// @flow
export {
  assertDataType,
  assertDataTypeClass,
  castData,
  getConstructor,
  getConstructorName,
  getDataLength,
  assertInteger,
  assertNotEmptyString,
  assertB64StringWithSize
} from './data-types';

export type { Data, ResourceMetadata } from './data-types';
