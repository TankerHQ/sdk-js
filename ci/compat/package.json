{
  "name": "tankerSdkCompatibilityTests",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "test": "mocha \"packages/**/index.spec.js\" --exit --recursive --full-trace --trace-warnings --reporter=spec --require ../../config/tests/babel.setup.js"
  },
  "devDependencies": {
    "mocha": "^5.2.0"
  },
  "workspaces": {
    "packages": ["packages/*"],
    "nohoist": ["**/@tanker/*"]
  }
}
